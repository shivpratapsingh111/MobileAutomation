id: Stripe_Restricted_API_Key

info:
  name: Stripe_Restricted_API_Key
  author: Shiv
  severity: medium

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "rk_live_[0-9a-zA-Z]{24}"
