id: AppCenter_Secret

info:
  name: AppCenter_Secret
  author: Shiv
  severity: medium

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "AppCenter\\.(configure|start)\\(.*\\)"
