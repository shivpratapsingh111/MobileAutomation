id: Square_OAuth_Secret

info:
  name: Square_OAuth_Secret
  author: Shiv
  severity: medium

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "sq0csp-[0-9A-Za-z\\-_]{43}"
