id: Artifactory_Password

info:
  name: Artifactory_Password
  author: Shiv
  severity: medium

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "(?:\\s|=|:|"|^)AP[\\dABCDEF][a-zA-Z0-9]{8,}"
